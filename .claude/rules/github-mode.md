# GitHub連携状態: 設定済み

アプリケーション開発（backlog/develop/test/finalizeフェーズ）が利用可能。
gh コマンドを通常通り使用してよい。

**最初のメッセージ対応前に `/sync-target-repo` を実行すること。** target_repo の clone/pull、git user 設定、`.gitignore` 整備、gaido 管理ファイルの commit/push を行う。完了後に `/orchestration-guide`、`/phase-workflow`、`/gaido-menavi-character` に進む。
